<%# locals: (unread_count:) %>
<div>
  <div class="text-sm font-semibold text-zinc-100">Notifications</div>
  <% if unread_count > 0 %>
    <div class="mt-0.5 text-xs text-zinc-400"><%= unread_count %> unread</div>
  <% end %>
</div>
<%= button_to destroy_all_notifications_path,
      method: :delete,
      class: "studs-nav-btn",
      style: "border-color: rgba(239,68,68,0.4); color: #fca5a5;",
      form: { data: { turbo_confirm: "Clear all notifications? This cannot be undone." } } do %>
  <svg xmlns="http://www.w3.org/2000/svg" class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
    <path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
  </svg>
  Clear
<% end %>
