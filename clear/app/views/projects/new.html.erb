<% content_for :page_title, "Groups" %>
<% content_for :page_subtitle, "Current group schedules" %>
<div class="rounded-2xl p-6" style="border: 1px solid var(--studs-border); background-color: var(--studs-panel-bg);">
  <div class="mb-4">
    <div class="text-base font-semibold text-zinc-100">New group</div>
    <div class="text-sm text-zinc-500">Create a new collaborative schedule.</div>
  </div>
  <%= render "form", project: @project, turbo: false %>
</div>
