<% content_for :page_title, @work_shift.title.presence || "Work Shift" %>
<% content_for :page_subtitle, "Shift details" %>
<% color = @work_shift.color.presence || "#34D399" %>
<div class="space-y-4">
  <!-- Header card -->
  <div class="rounded-2xl p-6"
       style="border: 1px solid var(--studs-border);
              background-color: var(--studs-panel-bg);
              border-left: 4px solid <%= color %>;">
    <div class="flex items-start justify-between gap-4">
      <div class="min-w-0 flex-1">
        <h1 class="text-2xl font-bold text-zinc-100 truncate min-w-0">
          <%= @work_shift.title.presence || "(Untitled)" %>
        </h1>
        <div class="mt-3 flex flex-wrap gap-4 text-sm text-zinc-400">
          <% if @work_shift.start_time.present? %>
            <span class="flex items-center gap-1.5">
              <svg class="h-4 w-4 opacity-60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
              </svg>
              <%= @work_shift.start_time.strftime("%-I:%M %p") %>
              <% if @work_shift.end_time.present? %>
                <span class="text-zinc-600">–</span>
                <%= @work_shift.end_time.strftime("%-I:%M %p") %>
              <% end %>
            </span>
          <% end %>
          <% if @work_shift.location.present? %>
            <span class="flex items-center gap-1.5">
              <svg class="h-4 w-4 opacity-60" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
                <path stroke-linecap="round" stroke-linejoin="round" d="M17.657 16.657L13.414 20.9a1.998 1.998 0 01-2.827 0l-4.244-4.243a8 8 0 1111.314 0z"/>
                <path stroke-linecap="round" stroke-linejoin="round" d="M15 11a3 3 0 11-6 0 3 3 0 016 0z"/>
              </svg>
              <%= @work_shift.location %>
            </span>
          <% end %>
        </div>
        <% if Array(@work_shift.repeat_days).any? %>
          <div class="mt-3 inline-flex items-center gap-1.5 rounded-full px-3 py-1 text-xs font-semibold"
               style="border: 1px solid var(--studs-border); background-color: var(--studs-panel-hover); color: var(--studs-accent);">
            <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15"/>
            </svg>
            <%= @work_shift.repeat_days_labels %>
            <% if @work_shift.repeat_until.present? %>
              until <%= @work_shift.repeat_until.strftime("%b %-d, %Y") %>
            <% end %>
          </div>
        <% end %>
      </div>
      <div class="flex shrink-0 items-center gap-1.5">
        <%= link_to edit_work_shift_path(@work_shift), class: "studs-nav-btn" do %>
          <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M11 5H6a2 2 0 00-2 2v11a2 2 0 002 2h11a2 2 0 002-2v-5m-1.414-9.414a2 2 0 112.828 2.828L11.828 15H9v-2.828l8.586-8.586z"/>
          </svg>
          Edit
        <% end %>
        <%= button_to work_shift_path(@work_shift),
              method: :delete,
              form: { data: { turbo_confirm: "Delete this shift?" } },
              class: "studs-nav-btn",
              style: "border-color: rgba(239,68,68,0.4); color: #fca5a5;" do %>
          <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
            <path stroke-linecap="round" stroke-linejoin="round" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
          </svg>
          Delete
        <% end %>
      </div>
    </div>
  </div>
  <!-- Description card -->
  <div class="rounded-2xl p-5"
       style="border: 1px solid var(--studs-border); background-color: var(--studs-panel-bg-2);">
    <div class="text-[10px] font-bold uppercase tracking-widest mb-3"
         style="color: var(--studs-accent); opacity: 0.8;">Description</div>
    <% if @work_shift.description.present? %>
      <p class="whitespace-pre-wrap text-sm text-zinc-200 leading-relaxed"><%= @work_shift.description %></p>
    <% else %>
      <p class="text-sm text-zinc-500">No description provided.</p>
    <% end %>
  </div>
  <!-- Footer actions -->
  <div class="flex items-center gap-1.5 pt-2" style="border-top: 1px solid var(--studs-divider);">
    <%= link_to dashboard_path, class: "studs-nav-btn" do %>
      <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M10 19l-7-7m0 0l7-7m-7 7h18"/>
      </svg>
      Back to dashboard
    <% end %>
    <%= link_to work_shifts_path, class: "studs-nav-btn" do %>
      <svg class="h-3.5 w-3.5" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2.5">
        <path stroke-linecap="round" stroke-linejoin="round" d="M4 6h16M4 10h16M4 14h16M4 18h16"/>
      </svg>
      All shifts
    <% end %>
  </div>
</div>
